# 🐙 Hydra AI Platform — Архитектура и концепция

> **Последнее обновление:** Февраль 2025

---

## 📋 Концепция проекта

**Hydra** — многомодельная AI-платформа для коллегиальной работы с языковыми моделями. Ключевая идея: получать **параллельные ответы** от разных AI-агентов с уникальными ролями, системными промптами и настройками, а затем синтезировать оптимальное решение.

### Философия
- **Коллегиальность**: несколько экспертов лучше одного
- **Специализация**: каждая роль имеет чёткое назначение
- **Прозрачность**: видимость reasoning-процесса моделей
- **Модульность**: расширяемая архитектура компонентов

---

## 🎯 Функциональные модули

### 1. Expert Panel (`/expert-panel`)
Основной интерфейс мультимодельного чата.

**Возможности:**
- Параллельные запросы к нескольким AI-моделям
- Per-model настройки (температура, max_tokens, системный промпт)
- Древовидная навигация по истории (`ChatTreeNav`)
- Rich-рендеринг: Markdown, LaTeX, syntax highlighting, Mermaid
- Блоки размышлений (Thinking) с переводом

**Ключевые компоненты:**
- `ChatInputArea` — ввод, загрузка файлов, paste handling
- `ChatMessagesList` — рендеринг, прокрутка, группировка
- `ConsultantPanel` — боковая панель для D-Chat
- `ModelSelector` / `MultiModelSelector` — выбор моделей

### 2. Ролевая система AI-агентов

#### Эксперты (участвуют в коллегиальных обсуждениях)
| Роль | ID | Назначение |
|------|----|------------|
| Ассистент | `assistant` | Универсальный эксперт |
| Критик | `critic` | Критический анализ, поиск слабых мест |
| Арбитр | `arbiter` | Синтез точек зрения, финальное решение |
| Консультант | `consultant` | Глубокий экспертный ответ |
| Модератор | `moderator` | Модерация дискуссии, подведение итогов |
| Советник | `advisor` | Стратегические рекомендации |
| Аналитик | `analyst` | Анализ данных, выявление паттернов |
| Веб-хантер | `webhunter` | Поиск и агрегация информации |

#### Технический персонал (агенты-помощники пользователя)
| Роль | ID | Назначение |
|------|----|------------|
| Промпт-инженер | `promptengineer` | Создание и оптимизация промптов |
| Регулятор потоков | `flowregulator` | Проектирование data-flow пайплайнов |
| Архивариус | `archivist` | Управление библиотеками и памятью |

### 3. D-Chat (Консультант-панель)
Боковая выдвижная панель для одиночных экспертных запросов.

- Режим **Moderator**: агрегация контекста сессии
- Перенос результатов в основной чат
- Независимая от основного потока работа

### 4. Tasks (`/tasks`)
Централизованный хаб управления сессиями.

- Создание задач с конфигурацией моделей
- Sheet-интерфейс для настройки `session_config`
- Мульти-селектор с категоризацией моделей

### 5. Библиотеки

#### Prompt Library (`/role-library`)
- CRUD для системных промптов
- Фильтрация по роли, владельцу
- Shared/Personal режимы
- Привязка к ролям агентов

#### Tools Library (`/tools-library`)
- **Prompt-Template**: параметризованные шаблоны
- **HTTP API**: интеграция с внешними сервисами
- SSRF-защита, таймаут 30с, лимит 100KB
- Встроенный тестер (`HttpToolTester`)

### 6. Staff Roles (`/staff-roles`)
Управление штатом AI-специалистов.

- Просмотр всех ролей с группировкой (Эксперты / Техперсонал)
- Сворачиваемые категории (шевроны)
- Детальная панель с системным промптом
- Интеграция с Prompt Library (загрузка/сохранение)

### 7. Flow Editor (`/flow-editor`)
Визуальный редактор AI-пайплайнов.

**Типы нод (18+):**
- Input, Output, Model, Prompt
- API, Database, Storage
- Condition, Switch, Loop
- Split, Merge, Filter, Transform
- Delay, Memory, Embedding, Classifier, Group

**Экспорт:** PNG, SVG, JSON, PDF

### 8. Hydrapedia (`/hydrapedia`)
База знаний платформы с Markdown-контентом.

---

## 🔧 Техническая архитектура

### Frontend Stack

```
React 18 + TypeScript + Vite
├── UI: shadcn/ui + Radix UI + Tailwind CSS
├── State: TanStack Query + React Context
├── Routing: React Router v6 (lazy loading)
├── Charts: Recharts
├── Flow: @xyflow/react + dagre
├── Markdown: react-markdown + remark-gfm + KaTeX
└── Animation: framer-motion
```

### Backend (Lovable Cloud)

```
Supabase
├── Auth: Email/password authentication
├── Database: PostgreSQL с RLS
├── Edge Functions: Deno runtime
├── Vault: Secure API key storage
└── Storage: File attachments
```

### Модульная структура

```
src/
├── components/
│   ├── flow/           # Flow Editor (nodes, edges, panels)
│   ├── hydrapedia/     # Documentation renderer
│   ├── layout/         # Layout, AppSidebar
│   ├── profile/        # UsageStats
│   ├── staff/          # RoleDetailsPanel
│   ├── tools/          # HttpToolTester
│   ├── ui/             # shadcn/ui + custom
│   └── warroom/        # Chat components (20+ files)
├── config/
│   └── roles.ts        # Unified role configuration
├── contexts/           # Auth, Theme, Language
├── hooks/              # 20+ custom hooks
├── lib/                # Utilities (fileUtils, imageCompression, etc.)
├── pages/              # Route pages (12 pages)
├── styles/             # DESIGN_SYSTEM.md
└── types/              # TypeScript definitions

supabase/functions/
├── hydra-orchestrator/ # AI routing, tools, streaming
│   ├── index.ts
│   ├── types.ts
│   ├── constants.ts
│   ├── tools.ts
│   └── http-executor.ts
└── translate-text/     # Translation service
```

### База данных

| Таблица | Назначение |
|---------|------------|
| `sessions` | Задачи/сессии с `session_config` |
| `messages` | История чата с metadata |
| `profiles` | Пользовательские профили |
| `user_api_keys` | Vault-ссылки на API ключи |
| `user_roles` | Роли доступа (user/moderator/admin/supervisor) |
| `prompt_library` | Библиотека системных промптов |
| `custom_tools` | Кастомные инструменты |
| `flow_diagrams` | Сохранённые диаграммы |
| `model_presets` | Пресеты моделей |
| `model_statistics` | Статистика использования |

---

## 🤖 AI-провайдеры

| Провайдер | Доступ | Модели |
|-----------|--------|--------|
| **Lovable AI** | Встроенный | Gemini 2.5/3, GPT-5/5.2 |
| **OpenAI** | Personal key | GPT-4o, o1, o3-mini |
| **Anthropic** | Personal key | Claude 3.5 Sonnet/Haiku |
| **Google** | Personal key | Gemini 1.5/2.0 |
| **xAI** | Personal key | Grok 3 family |
| **Groq** | Personal key | Llama 3.x, Mixtral (⚡fast) |
| **OpenRouter** | Personal key | Free tier models |

### Кэширование доступности
- `modelAvailabilityCache` в localStorage
- TTL 1 час для недоступных моделей
- Кнопка сброса кэша в UI

---

## 🔐 Безопасность

- **RLS-политики** на всех пользовательских таблицах
- **Vault** для API-ключей (не plaintext)
- **SSRF-защита** в HTTP-инструментах
- **Ролевая модель**: user → moderator → admin → supervisor

---

## 🌍 Локализация

- Полная поддержка **RU/EN**
- Централизованный словарь в `LanguageContext`
- Динамическое переключение без перезагрузки

---

## 🎨 Дизайн-система

Документирована в `src/styles/DESIGN_SYSTEM.md`:
- Семантические токены (--background, --foreground, --primary, etc.)
- Типографика с системой размеров
- Компонентные варианты (button, card, badge)

---

## 🚀 Roadmap

| Приоритет | Направление | Описание |
|-----------|-------------|----------|
| 🔴 High | Streaming responses | Real-time отображение генерации |
| 🔴 High | RAG-интеграция | Vector store для контекстного поиска |
| 🟡 Medium | Workflow automation | Запуск Flow-диаграмм как пайплайнов |
| 🟡 Medium | Voice I/O | Speech-to-text / text-to-speech |
| 🟢 Low | Team collaboration | Shared workspaces, real-time cursors |
| 🟢 Low | Plugin ecosystem | Расширяемая архитектура |
| 🟢 Low | Mobile PWA | Оптимизация для мобильных |

---

## 📝 Архитектурные решения

### Модульность
- Декомпозиция больших компонентов на focused-модули
- Выделение бизнес-логики в специализированные хуки
- Единый источник типов (`src/types/index.ts`)

### Паттерны
- `ResizablePanelGroup` для ресайз-интерфейсов
- `Sheet` для модальных форм настройки
- `TanStack Query` для серверного состояния
- `React Context` для глобального UI-состояния

### Conventions
- Файлы компонентов: PascalCase
- Хуки: camelCase с префиксом `use`
- Типы: PascalCase, суффикс для интерфейсов не обязателен
- Локализация: dot-notation ключи (`staffRoles.expertsGroup`)
