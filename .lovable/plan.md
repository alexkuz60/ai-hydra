
# План: Таймаут 120 секунд с диалогом выбора действий

## Задача
При превышении 120 секунд ожидания ответа от модели скелетон-индикатор анимированно трансформируется в диалог выбора действий:
- "Персональный запрос" — повторно отправить запрос только этому эксперту
- "Забыть" — удалить скелетон без последствий
- "Уволить" — удалить модель из списка выбранных для дальнейшей работы

## Архитектура решения

```text
┌─────────────────────────────────────────────────────────────────┐
│                    ExpertPanel.tsx                              │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ PendingResponseState                                        ││
│  │ + status: 'timedout' (новый статус)                        ││
│  └─────────────────────────────────────────────────────────────┘│
│                           │                                     │
│         onRetry   onDismiss   onRemoveModel                     │
│                           │                                     │
│                           ▼                                     │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │       MessageSkeleton.tsx (обновление)                      ││
│  │  - При status === 'timedout' показывает диалог              ││
│  │  - Три кнопки с иконками и анимацией                        ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

## Поток данных

1. Таймер обновляет `elapsedSeconds` каждую секунду
2. При `elapsedSeconds >= 120` статус меняется на `'timedout'`
3. `MessageSkeleton` переключается на отображение диалога
4. По выбору пользователя:
   - "Персональный запрос" → `onRetry(modelId)` → повторный вызов оркестратора
   - "Забыть" → `onDismiss(modelId)` → удаление из `pendingResponses`
   - "Уволить" → `onRemoveModel(modelId)` → удаление из `selectedModels`

## Файлы для изменения

### 1. Обновить: `src/types/pending.ts`

Добавить новый статус `'timedout'`:

```typescript
export interface PendingResponseState {
  modelId: string;
  modelName: string;
  role: AgentRole;
  status: 'sent' | 'confirmed' | 'waiting' | 'timedout';  // + timedout
  startTime: number;
  elapsedSeconds: number;
}
```

### 2. Обновить: `src/components/warroom/MessageSkeleton.tsx`

Добавить отображение диалога при таймауте:

```typescript
interface MessageSkeletonProps {
  pending: PendingResponseState;
  onRetry?: (modelId: string) => void;
  onDismiss?: (modelId: string) => void;
  onRemoveModel?: (modelId: string) => void;
}
```

Визуальное оформление при `status === 'timedout'`:
- Анимация fade-in для перехода от скелетона к диалогу
- Иконка Coffee/CoffeeOff (перекур)
- Заголовок: "Эксперт [модель] убежал на перекур.."
- Три кнопки в ряд:
  - "Персональный запрос" (RefreshCw) — primary style
  - "Забыть" (X) — ghost style
  - "Уволить" (UserMinus) — destructive outline

### 3. Обновить: `src/pages/ExpertPanel.tsx`

Добавить обработку таймаута в таймере и три callback-а:

```typescript
// В таймере: при elapsed >= 120 устанавливаем status = 'timedout'
if (elapsed >= 120) status = 'timedout';

// Новые обработчики
const handleRetryRequest = useCallback(async (modelId: string) => {
  // Получить последнее сообщение пользователя
  // Вызвать оркестратор только для этой модели
  // Сбросить статус pending обратно в 'sent'
}, []);

const handleDismissTimeout = useCallback((modelId: string) => {
  // Просто удалить из pendingResponses
}, []);

const handleRemoveModel = useCallback((modelId: string) => {
  // Удалить из pendingResponses
  // Удалить из selectedModels
}, [setSelectedModels]);
```

Передать callbacks в `ChatMessagesList`:

```typescript
<ChatMessagesList
  pendingResponses={pendingResponses}
  onRetryRequest={handleRetryRequest}
  onDismissTimeout={handleDismissTimeout}
  onRemoveModel={handleRemoveModel}
/>
```

### 4. Обновить: `src/components/warroom/ChatMessagesList.tsx`

Прокинуть новые props в `MessageSkeleton`:

```typescript
interface ChatMessagesListProps {
  pendingResponses?: Map<string, PendingResponseState>;
  onRetryRequest?: (modelId: string) => void;
  onDismissTimeout?: (modelId: string) => void;
  onRemoveModel?: (modelId: string) => void;
}
```

### 5. Обновить: `src/hooks/useSendMessage.ts`

Добавить функцию `retrySingleModel` для персонального повторного запроса:

```typescript
const retrySingleModel = useCallback(async (
  modelId: string, 
  messageContent: string
) => {
  // Подготовить только одну модель
  // Вызвать оркестратор
  // Вернуть результат
}, []);

return {
  // ... existing
  retrySingleModel,
};
```

### 6. Обновить: `src/contexts/LanguageContext.tsx`

Добавить переводы:

```typescript
'skeleton.timedout': { 
  ru: 'Эксперт {model} убежал на перекур..', 
  en: 'Expert {model} went for a break..' 
},
'skeleton.retryRequest': { ru: 'Персональный запрос', en: 'Personal request' },
'skeleton.dismiss': { ru: 'Забыть', en: 'Dismiss' },
'skeleton.removeModel': { ru: 'Уволить', en: 'Remove' },
```

## Дизайн диалога таймаута

```text
┌─────────────────────────────────────────────────────────────────┐
│  ☕ Эксперт GPT-5 убежал на перекур..                           │
│─────────────────────────────────────────────────────────────────│
│                                                                 │
│   ┌─────────────────┐  ┌─────────┐  ┌─────────────┐            │
│   │ 🔄 Персональный │  │ ✕ Забыть│  │ 👤- Уволить │            │
│   │     запрос      │  │         │  │             │            │
│   └─────────────────┘  └─────────┘  └─────────────┘            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

- Карточка с amber/warning стилем
- Иконка Coffee вместо роли
- Анимированный переход от скелетона (animate-fade-in)
- Кнопки с hover-эффектами

## Технические детали

### Условие в таймере ExpertPanel:
```typescript
for (const [key, value] of updated) {
  const elapsed = Math.floor((now - value.startTime) / 1000);
  let status: 'sent' | 'confirmed' | 'waiting' | 'timedout' = 'sent';
  
  if (elapsed >= 120) status = 'timedout';
  else if (elapsed >= 5) status = 'waiting';
  else if (elapsed >= 2) status = 'confirmed';
  
  updated.set(key, { ...value, elapsedSeconds: elapsed, status });
}
```

### Логика повторного запроса:
1. Найти последнее сообщение пользователя в `messages`
2. Получить настройки модели из `perModelSettings`
3. Сбросить `startTime` и `status` в pending
4. Вызвать `retrySingleModel` с контентом и modelId

## Результат

- После 120 секунд ожидания скелетон плавно превращается в диалог выбора
- Супервизор может повторить запрос, проигнорировать таймаут, или удалить модель из сессии
- Все действия имеют визуальную обратную связь через toast-уведомления
