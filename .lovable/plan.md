# План: Обновление Гидропедии

## Текущее состояние

Гидропедия содержит **17 разделов** документации:

| ID | Название | Статус |
|----|----------|--------|
| `intro` | Что такое AI-Hydra? | ✅ Актуален |
| `getting-started` | Начало работы | ⚠️ Обновить API-ключи |
| `expert-panel` | Панель экспертов | ⚠️ Добавить Session Memory UI |
| `streaming-mode` | Режим стриминга | ✅ Актуален |
| `prompt-library` | Библиотека промптов | ✅ Актуален |
| `tools` | Инструменты | ⚠️ Добавить новые HTTP-инструменты |
| `flow-editor` | Редактор потоков | 🔴 Требует обновления (Runtime) |
| `model-ratings` | Рейтинг моделей | ✅ Актуален |
| `best-practices` | Лучшие практики | ✅ Актуален |
| `tasks` | Управление задачами | ⚠️ Проверить актуальность |
| `roles-catalog` | Каталог ролей | ✅ Актуален |
| `d-chat-moderator` | D-Chat модератор | ✅ Актуален |
| `session-memory` | Память сессии | ⚠️ Обновить UI управления |
| `web-search` | Веб-поиск | ✅ Актуален |
| `localization` | Локализация | ✅ Актуален |
| `security` | Безопасность | ✅ Актуален |
| `behavioral-patterns` | Поведенческие паттерны | ✅ Актуален |
| `hydra-training` | Дрессировка Гидры (admin) | ⚠️ Добавить Flow Runtime |

---

## Приоритетные обновления

### 1. Редактор потоков (`flow-editor`) — ВЫСОКИЙ ПРИОРИТЕТ

Добавить документацию по Flow Runtime:

```markdown
## Выполнение потоков (Flow Runtime)

### Панель выполнения
- Кнопка запуска / остановки
- Прогресс-индикатор узлов
- Визуализация данных на связях

### Поддерживаемые узлы
| Тип | Описание |
|-----|----------|
| Input | Входные данные потока |
| Model | Вызов AI-модели |
| Prompt | Формирование промпта |
| Condition | Условное ветвление |
| Transform | Преобразование данных |
| Output | Результат потока |
| Database | CRUD операции |
| API | HTTP-запросы |
| Delay | Задержка выполнения |
| Checkpoint | Ожидание подтверждения |
| Loop | Циклы обработки |
| Split/Merge | Параллельная обработка |
| Classifier | Классификация текста |
| Embedding | Генерация эмбеддингов |
| Memory | Работа с памятью |
| Storage | Файловое хранилище |
```

### 2. Память сессии (`session-memory`) — СРЕДНИЙ ПРИОРИТЕТ

Обновить раздел с новым UI:

- Диалог управления памятью (SessionMemoryDialog)
- Фильтрация по типам чанков
- Семантический поиск с Similarity Score
- Обнаружение дубликатов
- Массовое удаление

### 3. Начало работы (`getting-started`) — НИЗКИЙ ПРИОРИТЕТ

Обновить таблицу провайдеров:
- Добавить Perplexity, Brave Search
- Обновить ссылки на консоли провайдеров
- Указать поддерживаемые модели (GPT-5, Gemini 2.5/3)

### 4. Инструменты (`tools`) — НИЗКИЙ ПРИОРИТЕТ

Добавить раздел HTTP-инструментов:
- Создание кастомных HTTP-инструментов
- Параметры запроса (метод, заголовки, тело)
- Тестирование через HttpToolTester

---

## Формат изменений

Каждое обновление включает:
1. Русскую версию (`ru`)
2. Английскую версию (`en`)
3. Таблицы элементов UI с иконками
4. Mermaid-диаграммы где уместно

---

## Технические требования

- Синтаксис `@role` для иконок ролей
- Иконки из Lucide в обратных кавычках
- Диаграммы Mermaid без символа `@` в названиях
- Локализация ключей в LanguageContext

---

## Следующие шаги

1. [ ] Обновить раздел `flow-editor` — добавить Flow Runtime
2. [ ] Обновить раздел `session-memory` — новый UI
3. [ ] Обновить `getting-started` — новые провайдеры
4. [ ] Обновить `hydra-training` — Flow Runtime для админов
5. [ ] Проверить все Mermaid-диаграммы на корректность
