# Hydra Evolution Strategy — ReAct Integration

> **Паттерн**: ReAct (Reasoning + Acting) — синергия рассуждений и действий  
> **Цель**: Превращение Эволюционера из генератора текста в автономного исследователя с верифицируемой логикой  
> **Статус**: Фаза 1 — Структурированные траектории ✅ (реализовано в `evolution-trigger`)

---

## 🧬 Три вектора эволюции

### Фаза 1: Структурированные траектории (Фундамент)

**Сложность**: Средняя  
**Приоритет**: Критический — база для всех последующих фаз

Формализация цикла `Think → Act → Observe` в каждой ревизии Эволюционера.

**Реализация**:
- Расширение `ai_revision` в таблице `chronicles` до структурированного JSON с траекторией рассуждений
- Каждый шаг траектории генерирует запись в `role_memory` (тип: `experience`)
- Формат траектории:
  ```json
  {
    "trajectory": [
      { "step": "think", "content": "Анализ: системный промпт роли X содержит конфликт..." },
      { "step": "act", "content": "Поиск в role_knowledge по категории 'system_prompt'..." },
      { "step": "observe", "content": "Найдено 3 релевантных записи, подтверждающих гипотезу..." }
    ],
    "revision": "...",
    "confidence": 0.85
  }
  ```

**Связь с памятью**: Траектории создают данные → обогащают `role_memory` → доступны для будущих ревизий через `search_role_memory`

---

### Фаза 2: Верификация гипотез (Расследование) ✅ (реализовано в `evolution-trigger`)

**Сложность**: Высокая  
**Приоритет**: Высокий — устраняет «слепые» ревизии

Эволюционер анализирует существующую память Hydra перед формулировкой ревизии.

**Реализация**:
- Генерация эмбеддингов через Lovable AI Gateway (`text-embedding-004`) для семантического поиска
- Интеграция с RAG-стеком: `hybrid_search_role_knowledge`, `search_role_memory` (vector)
- Новый шаг `VERIFY` в траектории: Think → Act → **Verify** → Observe → Revise
- Анализ success rate прошлых ревизий и извлечение успешных/неуспешных стратегий
- Оценка риска (low/medium/high) на основе доступного контекста
- Graceful fallback на текстовый поиск при недоступности эмбеддингов
- Модульная архитектура: `types.ts`, `ai.ts`, `memory.ts`, `index.ts`

**Связь с памятью**: Верификация проверяет данные из Фазы 1 → формирует обоснованные ревизии → результаты возвращаются в память

---

### Фаза 3: Самокоррекция (Иммунная система) ✅ (реализовано в `evolution-trigger`)

**Сложность**: Очень высокая  
**Приоритет**: Стратегический — замыкает петлю обратной связи

Механизм обработки исключений и мета-обучения на архиве ревизий.

**Реализация**:
- Логирование результатов ревизий как `success` / `mistake` в `role_memory` с полными метаданными (strategy_tags, confidence, role_object)
- Модуль `meta.ts`: анализ strategy_tags по success_rate, формирование рекомендаций и предупреждений
- Инъекция мета-контекста в THINK-промпт: рекомендуемые и избегаемые стратегии со статистикой
- Автоматическая коррекция confidence через `meta_confidence_modifier` (от -0.15 до +0.1)
- Новый режим `record_outcome` в edge-функции для записи результатов резолюции Супервизора
- UI автоматически вызывает `record_outcome` при одобрении/отклонении revised-записей
- Graceful degradation: при отсутствии данных мета-обучение просто не влияет на результат

**Связь с памятью**: Самокоррекция корректирует стратегию на основе накопленных данных → влияет на будущие траектории (Фаза 1)

---

## 🔄 Единый цикл рефлексии

```
Траектории (создают данные)
       ↓
Верификация (проверяет данные)
       ↓
Самокоррекция (корректирует стратегию)
       ↓
Новые траектории (с учётом опыта)
```

Все три фазы замыкаются через структуру памяти Hydra:
- `role_memory` — операционная память (опыт, ошибки, успехи)
- `role_knowledge` — база знаний (документация, эталоны)
- `chronicles` — журнал эволюции (траектории, метрики, резолюции)

---

## 📚 Источник

Исследование **ReAct: Synergizing Reasoning and Acting in Language Models** — демонстрирует, что чередование рассуждений и действий снижает галлюцинации, повышает интерпретируемость и достоверность решений LLM.

Применимость к Hydra: существующий `hydra-orchestrator` уже реализует ReAct-подобный паттерн через Tool Calling. Стратегия эволюции распространяет этот подход на механизм самосовершенствования.

---

## 🎯 Фокус реализации

> **Приоритет Фазы 1**: Эволюция ролевых системных промптов — минимизация «ошибок атрибуции» (отделение проблем промпта от неясных запросов пользователя или выбора модели).

> **Принцип**: Расширение через метаданные и промпты, а не через хардкод.

---

## 🏗️ Архитектурная эволюция

### [2026-02-20] Модуляризация Hydra Memory Hub

**Контекст:** Монолитный файл `HydraMemory.tsx` (4225 строк) разложен на 11 модулей в `src/components/memory/`.

**Результат:**
- Страница-оболочка: ~115 строк (делегация табам)
- `shared.tsx`: общие токены, утилиты (`matchRoleKey`, `parseAiRevision`), компоненты (`StatCard`, `RoleBadge`)
- 8 табов-компонентов: `SessionMemoryTab`, `RoleMemoryTab`, `KnowledgeTab`, `CognitiveArsenalTab`, `MemoryGraphTab`, `DualGraphsTab`, `StorageTab`, `ChroniclesTab`, `RagDashboardTab`
- `index.ts`: barrel-экспорт

**Связь с эволюцией:** Модульная архитектура упрощает автономное обновление отдельных когнитивных подсистем Эволюционером — каждый таб может быть изменён независимо без риска регрессий в других модулях. Это прямой вклад в инфраструктуру самосовершенствования (Фаза 2).
