# Hydra Changelog

История изменений проекта для документирования в Гидропедии.

---

## [2026-02-28] Контур B: Триггер деградации качества моделей (v0.2.22-alpha)

### Новое
- **Контур B Эволюционера** — триггер `trg_model_degradation` на `model_statistics` (AFTER UPDATE): при `arbiter_score < 6.0` у последних 3 оценок подряд для одной модели генерируется `supervisor_notifications` с `entry_code = 'model_degradation'`
- **Содержание уведомления**: полный `model_id` (провайдер/модель), роль (`role_used`), средний балл за 3 оценки
- **Защита от дублей**: одно непрочитанное уведомление на модель — повторное не создаётся, пока предыдущее не прочитано
- **Хроника HYDRA-EVO-007**: запись о развёртывании Контура B с метриками и гипотезой

### Изменено
- `README.md` / `README_RU.md` — секция Эволюционера обновлена: Контур А + Контур B вместо «Контур А + Контур B (будущее)»
- `src/styles/DESIGN_SYSTEM.md` — контракт Автоактивации расширен описанием `trg_model_degradation`
- `src/content/hydrapedia/reference.ts` — Hydrapedia MAC-статья обновлена с двумя контурами
- Версия → `v0.2.22-alpha`

### Как работает
Контур B замыкает цикл эволюции: **Контур A** (устаревание знаний / смена модели) + **Контур B** (деградация производительности) = полная автоматизация мониторинга качества нанятого персонала. При обновлении `arbiter_score` триггер проверяет окно из 3 последних оценок (ORDER BY `last_used_at DESC`). Если все три < 6.0, Супервизор получает уведомление с рекомендацией переаттестации или замены.

### Связанные разделы Гидропедии
- Справочник → Архитектурные контракты (MAC) → Автоактивация Эволюционера
- Хроники Эволюции → HYDRA-EVO-007

---

## [2026-02-22] Полная локализация Memory Hub

### Новое
- **Двуязычный контент Хроник** — добавлены колонки `title_en`, `hypothesis_en`, `supervisor_comment_en`, `summary_en` в таблицу `chronicles`; все существующие записи (EVO-001 → EVO-CYCLE-005) переведены на английский
- **Локализованный опыт ролей** — записи `role_memory` отображают `metadata.content_en` при переключении на английский язык
- **Кастомный Calendar в фильтрах Хроник** — нативные `<input type="date">` (зависимые от ОС) заменены на shadcn Calendar с передачей `locale` (ru/enUS) из date-fns; добавлена кнопка «Очистить»/«Clear»

### Изменено
- `src/components/memory/ChroniclesTab.tsx` — хелпер `loc(ru, en)` для переключения контента; поиск по обоим языкам; Popover+Calendar вместо нативных дат
- `src/components/memory/RoleMemoryTab.tsx` — `getLocalizedContent()` + выборка `metadata` в запросе к Supabase

### Связанные разделы Гидропедии
- Память Гидры → Хроники Эволюции
- Память Гидры → Опыт ролей

---

## [2026-02-22] Реорганизация профиля, DotPoint роутер, облачная персистентность

### Новое
- **Два блока вкладок в Профиле**: «Личное» (Профиль, Настройки, Уведомления, Финансы) и «Управление API» (Ключи, Роутеры, Статистика) — независимое сохранение активных вкладок
- **DotPoint Dashboard** — полноценный дашборд российского AI-роутера (каталог, тесты, аналитика, логи, настройки)
- **Lovable AI таб** — панель встроенных моделей для администраторов в секции роутеров
- **Схлопывание списков моделей** — независимое сворачивание нативных и пользовательских секций в каталогах ProxyAPI и DotPoint (Collapsible)
- **Облачная персистентность тестов** — результаты тестов моделей сохраняются через `useCloudSettings` и не теряются при переключении вкладок
- **Индивидуальные кнопки сохранения** — каждый роутер (OpenRouter, ProxyAPI, DotPoint) имеет свою кнопку «Сохранить»
- **Финансы** — таб с оценкой расходов по провайдерам (токены, запросы, стоимость)
- **Виджет баланса OpenRouter** — отображение дневного/месячного расхода и остатка

### Изменено
- `src/pages/Profile.tsx` → декомпозиция на табы: `ProfileTab`, `PreferencesTab`, `ApiKeysTab`, `NotificationsTab`, `FinanceTab`, `ApiRoutersTab`
- `src/components/profile/proxyapi/ProxyCatalogSection.tsx` — добавлены схлопываемые секции
- `src/components/profile/dotpoint/DotPointCatalogSection.tsx` — аналогичные схлопываемые секции
- `src/hooks/useProxyApiData.ts` — результаты тестов через `useCloudSettings`
- `src/hooks/useDotPointData.ts` — результаты тестов через `useCloudSettings`
- Удалены неиспользуемые переменные `expl` в каталогах роутеров

### Связанные разделы Гидропедии
- Начало работы → Профиль пользователя
- Справочник → API-Роутеры (ProxyAPI и DotPoint)
- Справочник → Интеграции и API

---

## [2026-02-20] Учебные примеры и посев данных эволюции

### Новое
- Задачи-примеры (`is_system: true`) — 4 системные сессии с 26 предзаполненными мультимодельными сообщениями
  - «Креативный брейнсторм» — стартап-идеи, RICE-скоринг, валидационные гипотезы
  - «Код-ревью» — рефакторинг React God Context, чеклист безопасности Supabase
  - «Продуктовая стратегия» — Q2 Roadmap (OKR), формула TIDE для техдолга
  - «Quick Q&A» — сравнение RAG-архитектур (HyDE vs Parent Document), промпт Техноарбитра
- Клонирование учебных примеров в личные задачи
- Защита системных задач от удаления (включая bulk-чистку) через RLS

### Изменено
- `src/components/tasks/TaskRow.tsx` — визуальное разделение на группы «Учебные примеры» и «Мои задачи»
- `src/components/tasks/TaskDeleteDialog.tsx` — фильтрация системных задач из списка удаления

### Как работает
Системные сессии наполнены реалистичными диалогами от разных моделей (Gemini 2.5 Pro, GPT-5, GPT-5-mini) в ролях Аналитика, Консультанта и Критика. Данные обеспечивают «тёплый старт» для RAG-памяти (сегменты decision/evaluation/context/instruction), Хроник Эволюции и механизма эволюции (Эволюционер + Арбитр).

### Связанные разделы Гидропедии
- Задачи → Учебные примеры
- Хроники Эволюции → Посев данных
- Hydra Memory Hub → RAG warm-start

---

## [2026-02-20] Модуляризация Hydra Memory Hub

### Новое
- `src/components/memory/shared.tsx` — дизайн-токены, утилиты (`matchRoleKey`, `parseAiRevision`, `findDuplicates`), компоненты `StatCard`, `RoleBadge`
- `src/components/memory/SessionMemoryTab.tsx` — таб памяти сессий с гибридным поиском
- `src/components/memory/RoleMemoryTab.tsx` — таб опыта ролей с confidence-метриками
- `src/components/memory/KnowledgeTab.tsx` — таб базы знаний (RAG) с дедупликацией
- `src/components/memory/CognitiveArsenalTab.tsx` — дашборд когнитивного арсенала (6 слоёв)
- `src/components/memory/MemoryGraphTab.tsx` — SVG-граф памяти
- `src/components/memory/DualGraphsTab.tsx` — двойные графы (память + арсенал)
- `src/components/memory/StorageTab.tsx` — файлохранилище по бакетам
- `src/components/memory/ChroniclesTab.tsx` — Хроники Эволюции с редактором промптов
- `src/components/memory/RagDashboardTab.tsx` — RAG-аналитика
- `src/components/memory/index.ts` — barrel-экспорт всех модулей

### Изменено
- `src/pages/HydraMemory.tsx` — сокращён с 4225 до ~115 строк (тонкая оболочка с делегацией табам)

### Как работает
Монолитный файл декомпозирован на 11 модулей. Общая инфраструктура (токены цветов, роль-бейджи, утилиты парсинга) централизована в `shared.tsx`. Каждый таб — изолированный компонент со своими зависимостями. Страница-оболочка управляет только навигацией между табами.

### Связанные разделы Гидропедии
- Хаб Памяти → Архитектура модулей
- Хроники Эволюции → EVO-004

---

## [2026-02-17] Аватар профиля, скрининг-интервью, история собеседований, синхронизация Подиума со Штатом

### Новое
- `src/components/profile/AvatarCropDialog.tsx` — Canvas-кроппер аватара: drag, scroll-zoom, slider масштаба, вывод 260×260 JPEG
- `src/hooks/useUserProfile.ts` — функция `resolveAvatarUrl()` для генерации 2-часовых signed URL вместо хранения долгосрочных ссылок
- `src/components/ratings/ScreeningPanel.tsx` — скрининг-интервью победителей конкурса: параллельный запуск (concurrency=2), Timeline, StepCards, VerdictSection, изоляция через `source_contest_id`
- `src/components/staff/InterviewHistoryTable.tsx` — таблица истории собеседований: группировка по брендам, столбец роутера (ProxyAPI/OpenRouter/Direct), бейдж среднего балла, удаление сессий
- Синхронизация Подиума со Штатом: найм через вердикт скрининга записывается в `role_assignment_history`, инвалидирует кэш `role-assignments-active`, значок 🛡️ (ShieldCheck) появляется в Штате мгновенно

### Изменено
- `src/pages/Profile.tsx` — сохраняет путь файла (`userId/avatar.jpg`) вместо signed URL; кнопка загрузки аватара открывает кроппер
- `src/content/hydrapedia/basics.ts` — обновлена документация профиля: аватар, кроппер, структура вкладок
- `src/content/hydrapedia/features.ts` — добавлен раздел «Скрининг-интервью»
- `src/content/hydrapedia/admin.ts` — добавлены разделы «История собеседований» и «Синхронизация Подиума со Штатом»

### Как работает
**Аватар**: файл кадрируется в Canvas-кроппере (260×260 JPEG), загружается в Storage `avatars` по пути `userId/avatar.jpg`. В `profiles.avatar_url` хранится путь. При загрузке профиля `resolveAvatarUrl()` генерирует fresh signed URL на 2 часа.

**Скрининг**: победители конкурса проходят пакетное тестирование (Брифинг → Тесты → Вердикт) без официального найма. Результаты привязаны к конкурсу через `source_contest_id`, не попадают в основную статистику персонала. При «Нанять» модель записывается в `role_assignment_history`, кэш ролей инвалидируется.

**История собеседований**: `InterviewHistoryTable` группирует сессии по провайдеру/бренду (распознавание игнорирует префиксы шлюзов). Столбец «Роутер» идентифицирует шлюз с иконкой. Поддерживается удаление сессий.

### Связанные разделы Гидропедии
- Профиль → Аватар пользователя
- Подиум → Скрининг-интервью
- Штат → История собеседований
- Штат → Синхронизация с Подиумом

---

## [2025-02-04] Обновление контента Гидропедии

### Изменено
- **Память сессии** — добавлена информация об управлении памятью в Панели экспертов, кнопках RefreshCw и Settings2, вкладке «Дубликаты»
- **Паттерны поведения** — документировано разделение ролевых паттернов на «Эксперты» и «Технический персонал», добавлено описание визуального таймлайна этапов с анимацией потока
- **Дрессировка Гидры** — добавлен раздел «Синхронизация табеля о рангах» с описанием правил симметрии и диалога разрешения конфликтов

### Связанные разделы Гидропедии
- Память сессии (session-memory)
- Паттерны поведения (behavioral-patterns)  
- Дрессировка Гидры (hydra-training)

---

## [2025-02-04] Реорганизация системы паттернов и Гидропедии

### Новое
- **Секция «Дрессировка Гидры»** в Гидропедии — детальная документация для администраторов о внутренних механизмах ИИ
- **Разделение ролевых паттернов** на «Эксперты» и «Технический персонал» на странице Паттерны поведения
- **Колонка `source`** в таблице `flow_diagrams` для различения пользовательских и сгенерированных из паттернов диаграмм

### Изменено
- Системные паттерны (стратегические и ролевые) скрыты от обычных пользователей — видны только администраторам
- Flow-диаграммы, сгенерированные из паттернов (`source: 'pattern'`), скрыты в списке «Открыть» для не-админов
- Секции Гидропедии с флагом `adminOnly` отображаются только для администраторов

### Архитектура доступа
| Компонент | Пользователь | Администратор |
|-----------|--------------|---------------|
| Паттерны поведения | Свои + публичные | Все, включая системные |
| Flow Editor «Открыть» | source='user' | Все диаграммы |
| Гидропедия | Без «Дрессировки» | Все секции |

---

## [2025-02-03] Семантический поиск в памяти сессии

### Новое
- Функция `generateEmbedding()` в useSessionMemory — генерирует эмбеддинги через edge-функцию
- Функция `semanticSearch()` — высокоуровневый поиск (генерация эмбеддинга → RPC search_session_memory)
- Переключатель режима поиска в SessionMemoryDialog (текстовый ↔ семантический)
- Отображение score релевантности (%) для результатов семантического поиска

### Изменено
- `src/hooks/useSessionMemory.ts` — добавлены generateEmbedding, semanticSearch
- `src/components/warroom/SessionMemoryDialog.tsx` — интеграция AI-поиска с анимированным toggle (Sparkles/Text)
- `src/components/warroom/ConsultantPanel.tsx` — передача onSemanticSearch в диалог
- `src/pages/ExpertPanel.tsx` — передача onSemanticSearch в диалог
- `src/contexts/LanguageContext.tsx` — переводы для семантического поиска

### Как работает
При активации семантического режима (кнопка Sparkles) запрос отправляется в edge-функцию generate-embeddings, которая возвращает вектор 1536 измерений. Вектор передаётся в RPC search_session_memory для поиска по косинусному расстоянию (pgvector). Результаты сортируются по similarity и отображаются с процентом релевантности.

### Связанные разделы Гидропедии
- Память сессии → Семантический поиск
- Edge Functions → generate-embeddings

---

## [2025-02-03] Управление памятью в основном чате

### Новое
- Кнопка «Освежить память» (RefreshCw) в заголовке ExpertPanel с анимацией успеха
- Кнопка «Управление памятью» (Settings2) для открытия диалога SessionMemoryDialog
- Интеграция SessionMemoryDialog в ExpertPanel — просмотр, фильтрация, удаление фрагментов

### Изменено
- `src/pages/ExpertPanel.tsx` — расширен useSessionMemory, добавлены состояния и обработчики управления памятью, интегрирован диалог

### Как работает
Кнопки управления памятью появляются в заголовке основного чата рядом с индикатором сохранённых фрагментов (🧠 N). Кнопка обновления запускает refetch с анимированным feedback (галочка на 2 сек). Диалог управления позволяет фильтровать по типам, искать дубликаты и удалять отдельные или все фрагменты — аналогично D-чату.

### Связанные разделы Гидропедии
- Панель экспертов → Управление памятью сессии
- D-Chat → Память сессии

---

## [2025-02-03] Поиск дубликатов в памяти сессии

### Новое
- Функция `findDuplicates()` в SessionMemoryDialog для обнаружения идентичных фрагментов
- Вкладка «Дубликаты» с подсчётом групп и подсветкой повторяющихся записей
- Поле поиска по содержимому фрагментов

### Изменено
- `src/components/warroom/SessionMemoryDialog.tsx` — добавлен поиск, фильтр дубликатов, визуальная индикация (amber border, badge)
- `src/contexts/LanguageContext.tsx` — переводы для поиска и дубликатов

### Как работает
Контент нормализуется (trim + lowercase), группируется по хешу. Записи с >1 вхождением помечаются как дубликаты. При включении фильтра «Дубликаты» отображаются только повторяющиеся записи с указанием количества в группе.

### Связанные разделы Гидропедии
- Память сессии → Оптимизация контекста
- SessionMemoryDialog → Управление фрагментами

---

## [2025-02-03] Диалог управления памятью сессии

### Новое
- `src/components/warroom/SessionMemoryDialog.tsx` — модальное окно для просмотра и управления сохранёнными фрагментами
- Фильтрация по типам (message, summary, decision, context, instruction)
- Массовая очистка с подтверждением

### Изменено
- `src/components/warroom/ConsultantPanel.tsx` — добавлена кнопка управления (Settings2) и интеграция диалога
- `src/contexts/LanguageContext.tsx` — переводы для диалога памяти

### Как работает
Диалог загружает chunks через useSessionMemory и отображает в ScrollArea с анимированными переходами (framer-motion). Поддерживается удаление отдельных записей и полная очистка памяти сессии с двухступенчатым подтверждением.

### Связанные разделы Гидропедии
- D-Chat → Управление памятью
- Память сессии → SessionMemoryDialog

---

## [2025-02-03] Автоматическая синхронизация табеля о рангах

### Новое
- `src/lib/hierarchyConflictDetector.ts` — утилита обнаружения конфликтов иерархии
- `src/components/staff/ConflictResolutionDialog.tsx` — диалог разрешения противоречий

### Изменено
- `src/hooks/useRoleBehavior.ts` — добавлен `fetchAllBehaviors()` для загрузки всех иерархий
- `src/components/staff/RoleDetailsPanel.tsx` — интеграция проверки конфликтов при сохранении
- `src/components/patterns/BehaviorEditorDialog.tsx` — интеграция проверки конфликтов при сохранении
- `src/contexts/LanguageContext.tsx` — переводы для диалога конфликтов

### Как работает
При сохранении иерархии ролей система автоматически проверяет симметричность связей:
- `A.defers_to[B]` требует `B.challenges[A]` (начальник-подчинённый)
- `A.collaborates[B]` требует `B.collaborates[A]` (коллеги)

При обнаружении противоречий открывается диалог с предложением синхронизировать все затронутые роли в одной транзакции.

### Связанные разделы Гидропедии
- Штат специалистов → Табель о рангах
- Поведенческие паттерны → Редактор иерархии

---

## Формат записей

```markdown
## [YYYY-MM-DD] Название функции

### Новое
- `путь/к/файлу.ts` — краткое описание

### Изменено
- `путь/к/файлу.ts` — что изменено

### Удалено
- `путь/к/файлу.ts` — причина удаления

### Как работает
Техническое описание механизма работы в 2-4 предложениях.

### Связанные разделы Гидропедии
- Модуль → Раздел
```
