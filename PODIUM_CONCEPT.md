# Подиум ИИ-моделей — Стратегическая концепция

> Документ фиксирует видение Пользователя и анализ с предложениями. Будет обновляться по мере проработки.

---

## 1. Общая концепция

Подиум стратегически аналогичен **Штату специалистов**, но для ИИ-моделей. Его цель — помочь Пользователю с **оптимальным выбором ИИ-моделей** для решения конкретной Задачи.

Образно: *сколько будет голов у гидры и чьи ИИ-мозги в эти головы вложить.*

Три раздела Подиума взаимосвязаны:
- **Портфолио** накапливает историю → данные для **Рейтингов**
- **Конкурс** тестирует кандидатов → результаты попадают в **Портфолио** и **Рейтинги**
- **Рейтинги** агрегируют всё → помогают принимать решения при подборе команды

---

## 2. Портфолио ИИ-моделей (Досье)

Персональное досье и послужной список каждой модели:

- В каких **задачах** участвовала
- В какой **роли** выступала (Эксперт, Консультант, Критик, Арбитр и др.)
- Сколько **полезных идей** предложила (🧠 brains)
- **Оценки** человеком и Арбитром
- **Критика** от Критика
- Зафиксированные **факты галлюцинаций**
- Количество **отклонённых** ответов (dismissals)

### Текущее состояние

Таблица `model_statistics` уже хранит базовые метрики (response_count, total_brains, dismissal_count) per session. Для полноценного досье потребуется расширение.

### Предложения по расширению `model_statistics`

```
role_used           TEXT        -- роль, в которой модель участвовала
task_id             UUID        -- привязка к задаче
hallucination_count INTEGER     -- зафиксированные галлюцинации
arbiter_score       NUMERIC     -- оценка Арбитра (0-10)
critique_summary    TEXT        -- краткое резюме критики
```

---

## 3. Конкурс красоты ИИ-интеллекта

Тестирование выбранных Пользователем ИИ-моделей-кандидатов на предмет оптимальности для конкретной роли.

### Механика

1. Пользователь выбирает **задачу** и **роль** (Эксперт, Консультант, Критик, Арбитр...)
2. Выбирает **N моделей-кандидатов** из дефаулт-списка
3. Все модели получают **одинаковый промпт** (test prompt)
4. Ответы отображаются **бок о бок** для сравнения
5. Пользователь и/или Арбитр оценивают результаты

### Ценность

- Подсказывает **слабые стороны** модели при решении конкретной Задачи
- Помогает создать **уникальный системный промпт** кастом-варианта модели
- Арсенал заданий тест-конкурсов будет **расширяться** по мере необходимости

### Предлагаемые таблицы

```sql
-- Конкурсы
CREATE TABLE model_contests (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID NOT NULL,
  name            TEXT NOT NULL,
  test_prompt     TEXT NOT NULL,
  role_tested     TEXT NOT NULL,        -- expert, consultant, critic, arbiter...
  task_context    TEXT,                 -- описание задачи для контекста
  status          TEXT DEFAULT 'draft', -- draft, running, completed
  created_at      TIMESTAMPTZ DEFAULT now(),
  completed_at    TIMESTAMPTZ
);

-- Участники конкурса
CREATE TABLE contest_entries (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  contest_id      UUID REFERENCES model_contests(id) ON DELETE CASCADE,
  model_id        TEXT NOT NULL,
  response_text   TEXT,
  response_time_ms INTEGER,
  human_score     NUMERIC,              -- оценка пользователя (0-10)
  arbiter_score   NUMERIC,              -- оценка Арбитра (0-10)
  notes           TEXT,                 -- заметки пользователя
  created_at      TIMESTAMPTZ DEFAULT now()
);
```

---

## 4. Рейтинги ИИ-моделей

Сводная статистика по всем моделям для всех Задач пользователя.

### Инструменты обработки

- **Фильтры** — по модели, роли, задаче, периоду
- **Сводные итоги** — агрегаты по моделям и ролям
- **Кросс-таблицы** — модель × роль, модель × задача
- **Графики** — Bar/Pie для топ-10, тренды по времени

### Текущее состояние

Компонент `ModelStatsChart` уже отображает базовые графики (Bar/Pie) для топ-10 моделей с фильтрацией по периодам.

---

## 5. Фазы реализации (предварительно)

### Фаза 1 — Расширение Портфолио
- Расширить `model_statistics` (role_used, task_id, hallucination_count, arbiter_score)
- Карточка-досье модели с послужным списком
- Автоматический сбор данных из основного чата

### Фаза 2 — Конкурс красоты ИИ-интеллекта
- Таблицы `model_contests` и `contest_entries`
- Интерфейс создания арены
- Параллельный запрос к N моделям
- Сравнение ответов бок о бок

### Фаза 3 — Продвинутые рейтинги
- Кросс-таблицы модель × роль
- Рекомендации оптимального состава для новой задачи
- Экспорт статистики

---

## 6. Открытые вопросы

- Формат заданий для конкурсов: свободный промпт vs. шаблоны по типу роли?
- Автоматическая оценка Арбитром или только ручная?
- Привязка к конкретным задачам (sessions) или абстрактные тесты?
- Максимальное количество моделей-участников в одном конкурсе?
- Как обрабатывать кастом-модели (с персонализированными промптами)?

---

*Документ создан для фиксации концепции. Будет обновлён после внесения поправок Пользователем.*
