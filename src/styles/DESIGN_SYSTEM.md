# AI-Hydra Design System

## Цветовые токены

### Базовые
- `background` / `foreground` — основной фон и текст
- `card` / `card-foreground` — карточки
- `primary` / `primary-foreground` — акцентные элементы (cyan)
- `secondary` / `secondary-foreground` — вторичные элементы
- `muted` / `muted-foreground` — приглушённые элементы
- `destructive` / `destructive-foreground` — опасные действия

### Hydra-специфичные
- `hydra-glow` — неоновое свечение (cyan)
- `hydra-success` — успех (зелёный)
- `hydra-warning` — предупреждение (жёлтый)
- `hydra-critical` — критично (красный)

### AI-роли
| Роль | Токен | Цвет |
|------|-------|------|
| Ассистент | `hydra-success` | Зелёный |
| Критик | `hydra-critical` | Красный |
| Арбитр | `hydra-arbiter` | Золотой |
| Консультант | `hydra-consultant` | Янтарный |
| Модератор | `hydra-moderator` | Синий |
| Советник | `hydra-advisor` | Изумрудный |
| Архивист | `hydra-archivist` | Бронзовый |
| Аналитик | `hydra-analyst` | Индиго |
| Вебхантер | `hydra-webhunter` | Оранжевый |
| Эксперт | `hydra-expert` | Фиолетовый |

## Использование

### В Tailwind
```tsx
// Правильно — используем токены
<div className="bg-primary text-primary-foreground" />
<span className="text-hydra-success" />
<div className="border-hydra-glow" />

// Неправильно — прямые цвета
<div className="bg-cyan-500" /> // ❌
<span className="text-green-400" /> // ❌
```

### В CSS
```css
.my-element {
  background: hsl(var(--primary));
  color: hsl(var(--primary-foreground));
  border-color: hsl(var(--hydra-glow));
}
```

## Шрифты
- `font-sans` — Inter (основной)
- `font-mono` — JetBrains Mono (код)
- `font-rounded` — Quicksand (заголовки, лого)

## Анимации
- `animate-pulse-glow` — пульсирующее свечение
- `animate-fade-in` — плавное появление
- `animate-slide-up` — появление снизу

## Компоненты

### Свечение
```tsx
<Button className="hydra-glow" />     // Полное свечение
<Button className="hydra-glow-sm" />  // Слабое свечение
```

### Скроллбар
```tsx
<ScrollArea className="hydra-scrollbar" />
```

## «Мысли шире» — чеклист целостности

Протокол активируется командой **«Лав, мысли шире»** при любой задаче.
Цель: проверить контракты между слоями параллельно с основной работой.

### 1. Типы и интерфейсы
- [ ] Новый параметр добавлен в интерфейс → используется ли он во всех потребителях?
- [ ] Удалённое поле убрано из всех мест (компоненты, хуки, edge functions)?
- [ ] Опциональные поля (`?`) действительно опциональны, а не «забыли передать»?

### 2. Пропсы и компоненты
- [ ] Компонент принимает проп → рендерит/использует его?
- [ ] Проп передаётся из родителя → он реально существует в источнике?
- [ ] Деструктуризация пропсов совпадает с интерфейсом?

### 3. API-контракты (клиент ↔ edge function)
- [ ] Edge function принимает параметр → клиент его отправляет?
- [ ] Клиент отправляет данные → edge function их читает и использует?
- [ ] Формат ответа edge function совпадает с тем, что парсит клиент?

### 4. Состояние и побочные эффекты
- [ ] Состояние обновляется → UI реагирует на изменение?
- [ ] useEffect зависимости корректны (нет stale closures)?
- [ ] Очистка (cleanup) вызывается при размонтировании?

### 5. Локализация
- [ ] Новые строки добавлены и для `ru`, и для `en`?
- [ ] Нет инлайновых строк на одном языке (кроме fallback)?
- [ ] Используется `language === 'ru'`, а не `t('common.locale')`?

### 6. Критерий «достаточно хорошо»
> Не рефакторить стабильный код без причины. Изменение оправдано, если:
> - Есть воспроизводимый баг
> - Нарушен контракт между слоями
> - Дублирование приводит к рассинхрону
> - Пользователь явно запросил улучшение

### 7. Кросс-модульные архитектурные контракты

> **Перед реализацией любой задачи** агент ОБЯЗАН проверить, не затрагивает ли она
> существующие контракты. При команде «Лав, мысли шире» — полный аудит.

#### Обязательные контракты (MUST)

| Контракт | Правило | Модули-потребители |
|----------|---------|-------------------|
| **Выбор модели** | Любой модуль, отправляющий запрос к AI, ОБЯЗАН использовать `getModelInfo()` из `useAvailableModels` для определения `use_lovable_ai` и провайдера | СПРЗ (`useConceptInvoke`), Чат (`hydra-orchestrator`), Конкурсы, Собеседования |
| **Приоритет провайдеров** | Иерархия: Lovable AI → BYOK → OpenRouter → ProxyAPI/DotPoint. Флаг `proxyapi_priority` из профиля определяет порядок внешних шлюзов | Edge functions: `hydra-orchestrator`, `contest-*`, `interview-*` |
| **Поисковый провайдер** | Определяется через `get_my_api_key_status` (Tavily/Perplexity). Системный fallback: Tavily | СПРЗ, Чат (tool-вызовы) |
| **Локализация** | Проверка `language === 'ru'`, **не** через `t('locale')`. Новые строки — и для `ru`, и для `en` | Все UI-компоненты |
| **Дизайн-токены** | Запрещены прямые цвета (`text-white`, `bg-cyan-500`). Только семантические токены из `index.css` | Все компоненты |
| **Брифинг (Knowledge Pyramid)** | Сборка контекста для AI-роли ОБЯЗАНА использовать 3-уровневую фильтрацию: A (Global) → B (Organizational) → C (Expertise, только целевая роль). Запрещено передавать C-уровень чужой роли — это нарушает изоляцию контекста и вызывает межролевую предвзятость | Edge functions: `hydra-orchestrator` (сборка брифинга), `interview-*` (дельта-брифинг), `contest-*`, Hydrapedia UI (визуализация уровней) |
| **Сертификация персонала** | При изменении `role_knowledge` или промптов роли ОБЯЗАНА срабатывать проверка версионирования (`useKnowledgeVersioning`). Расхождение → бейдж «Обновлено» → предложение переаттестации | Штатное расписание (`StaffTable`), `RecertificationPanel`, `KnowledgeChangedBadge` |
| **Memory Hub (Hybrid Search)** | Поиск по памяти ОБЯЗАН использовать полный стек: BM25 + pgvector + RRF (k=60) → HyDE-смешивание (0.4 query + 0.6 hyde) → Gemini Reranking (0.7 rerank + 0.3 hybrid). Запрещено использовать только векторный поиск без reranking — это снижает семантическую точность | Edge functions: `hydra-orchestrator` (RAG-контекст), `hybrid_search_session_memory`, `hybrid_search_role_knowledge`, Memory Hub UI (8 зон) |
| **Эмбеддинги** | Генерация ОБЯЗАНА использовать `openai/text-embedding-3-small` (1536 dim) через `generate-embeddings`. Fallback: персональный ключ OpenAI. Пакетная обработка по 10 чанков | Edge function: `generate-embeddings`, Memory Hub (пакетная индексация), `role_knowledge`, `session_memory`, `plan_conclusions` |
| **Автоактивация Эволюционера** | Контур A: ≥2 обновлений `role_knowledge` без переаттестации ИЛИ смена дефолт-модели роли → `supervisor_notifications` (entry_code: `knowledge_drift` / `model_changed`). Бэкенд: триггеры `trg_knowledge_staleness`, `trg_model_change_staleness`. Клиент: хук `useKnowledgeStaleness` для предупреждений в D-Chat, СПРЗ, Конкурсах. **Контур B**: триггер `trg_model_degradation` на `model_statistics` — скользящее среднее `arbiter_score < 6.0` три раза подряд → `model_degradation`. Уведомление содержит полный `model_id` (провайдер/модель), роль и средний балл. Защита от дублей: одно непрочитанное уведомление на модель | Триггеры БД: `role_knowledge`, `user_settings`, `model_statistics`; Хук: `useKnowledgeStaleness`; UI: `StaffTable`, `KnowledgeChangedBadge`, D-Chat, СПРЗ, Конкурсы |
| **Оценка в Конкурсах и Дуэлях** | Итоговый балл ОБЯЗАН рассчитываться как взвешенная сумма: 👤 40% `user_score` + ⚖️ 60% `arbiter_score`. Арбитр определяется по цепочке приоритетов: (1) нанятый ТехноАрбитр (`useHiredTechnoArbiter`), (2) дефолт-модель роли `technoarbiter`, (3) системный fallback `google/gemini-2.5-flash`. Критерии оценки (`criteria_scores`) ОБЯЗАНЫ включать минимум `factuality`, `relevance`, `depth`. Ролеспецифичные критерии (напр. `bias_detection` для Критика) добавляются автоматически по `role_tested` | Конкурсы (`contest-runner`, `ContestSummary`), Дуэли (`duel-runner`), Рейтинги (`ModelStatsChart`), Досье (`CandidateDetail`) |
| **Маршрутизация моделей в Конкурсах** | Модели-участники ОБЯЗАНЫ маршрутизироваться через `getModelInfo()` с учётом иерархии провайдеров. Модели с префиксами `openai/`, `google/` → Lovable AI; остальные → BYOK/OpenRouter/шлюзы. Параллельный запрос к N моделям ограничен `MAX_CONCURRENCY = 2`. Каждый результат ОБЯЗАН фиксировать `response_time_ms`, `token_count`, `model_id` в `contest_results` | Edge functions: `contest-runner`, `duel-runner`; Хуки: `useContestSession`, `useDuelSession`; UI: `ContestArena`, `DuelTimeline` |
| **Собеседование (Interview Pipeline)** | Пайплайн ОБЯЗАН включать 3 фазы: (1) Briefing — сборка контекста через Knowledge Pyramid (A→B→C) + дельта из `source_contest_id`; (2) Testing — 3–7 ситуационных задач через `RoleTestPlugin` с SSE-стримингом прогресса; (3) Verdict — автоматический вердикт (Hire/Reject) с Arbiter Fallback (Gemini 2.5 Flash → Pro). Адаптивная логика: 3 повтора при таймаутах, 45с idle-timeout, сохранение частичных результатов. Батч-скрининг через `useScreeningSession` с `MAX_CONCURRENCY = 2` | Edge functions: `interview-briefing`, `interview-test-runner`, `interview-verdict`; Хуки: `useInterviewSession`, `useScreeningSession`; UI: `InterviewPanel`, `ScreeningDashboard` |
| **Победители и найм** | Победитель конкурса/дуэли (наивысший взвешенный балл) ОБЯЗАН помечаться иконкой 👑. При переходе к Собеседованию `source_contest_id` ОБЯЗАН передаваться для формирования дельта-брифинга. Успешный найм (Hire) → запись в `role_assignment_history` с `interview_session_id` и `interview_avg_score`. Нанятая модель автоматически становится дефолтом для роли в `user_settings` (`tech-role-defaults`) | Конкурсы → Собеседования → Штатное расписание; `role_assignment_history`, `user_settings`; UI: `CandidateDetail`, `StaffTable` |
| **Flow Runtime** | Исполнение Flow-диаграмм ОБЯЗАНО использовать DAG-планировщик (топологическая сортировка) с параллелизмом независимых веток. SSE-стриминг событий (`progress`, `node_complete`, `error`) ОБЯЗАТЕЛЕН для визуализации. Узлы Model ОБЯЗАНЫ использовать `google/gemini-3-flash-preview` по умолчанию и маршрутизироваться через иерархию провайдеров. Checkpoint-узлы приостанавливают исполнение до подтверждения пользователем. Результаты на связях визуализируются в реальном времени. Сброс результатов — автоматически при новом старте или вручную | Edge function: `flow-runtime`; Хуки: `useFlowDiagrams`, `useFlowExecution`; UI: `FlowCanvas`, `FlowNodeTypes`, `FlowDataPreview` |
| **D-Chat (Панель экспертов)** | Мультиролевая оркестрация ОБЯЗАНА изолировать контексты: каждый эксперт получает брифинг только своей роли (Knowledge Pyramid C-уровень). Запросы группируются по `request_group_id` для корреляции ответов. Миграция результатов из Конкурсов (`useContestMigration`) ОБЯЗАНА сохранять роль и модель-источник. Смена модели в сессии чата ОБЯЗАНА проверяться хуком `useKnowledgeStaleness` для предупреждения об устаревании. Выбор модели для роли — через `session_config` сессии с fallback на `tech-role-defaults` | Edge function: `hydra-orchestrator`; Хуки: `useChat`, `useContestMigration`, `useKnowledgeStaleness`; UI: `ExpertPanel`, `ChatMessage`, `ModelSelector` |
| **СПРЗ (Концептуальный пайплайн)** | Последовательная цепочка Визионер → Стратег → Патентовед ОБЯЗАНА накапливать контекст: результаты каждого этапа передаются следующему. Протокол автоочистки: перед повторным вызовом эксперта старые сообщения с тем же `concept_type` удаляются. Маршрутизация: модели `openai/`, `google/` → Lovable AI; остальные → BYOK. Дефолт: `google/gemini-3-flash-preview`. Пайплайн жёстко прерывается при ошибке на любом этапе. Утверждение результатов (approval) ОБЯЗАНО проверять синхронность таймстемпов кэша и последних ответов | Edge function: `hydra-orchestrator`; Хуки: `useConceptInvoke`, `useConceptApproval`; UI: `TaskDetailsPanel`, `ConceptSection`, `ApprovalToolbar` |
| **Статистика моделей** | Обновление `model_statistics` ОБЯЗАНО происходить из всех источников: (1) Чат — `response_count`, `total_brains`, `dismissal_count` per session; (2) Конкурсы — `contest_count`, `contest_total_score`, `criteria_averages` из `contest_results`; (3) Собеседования — `arbiter_score`, `arbiter_eval_count` из verdict. Агрегация `criteria_averages` — скользящее среднее по критериям. Поле `hallucination_count` инкрементируется при ручной фиксации пользователем. Данные используются для Досье модели и Рейтингов (Bar/Pie/Radar) | Чат (`hydra-orchestrator`), Конкурсы (`contest-arbiter`), Собеседования (`interview-verdict`); UI: `ModelStatsChart`, `CandidateDetail`, `ModelDossier` |

#### Как добавить новый контракт
1. Добавь строку в таблицу выше
2. Обнови соответствующую memory-ноту с пометкой `ОБЯЗАН / MUST`
3. Перечисли все модули-потребители — это триггер для кросс-проверки
```
