import type { HydrapediaSection } from './types';

export const guideSections: HydrapediaSection[] = [
  {
    id: 'guide-tours',
    titleKey: 'hydrapedia.sections.guideTours',
    icon: 'Compass',
    content: {
      ru: `# Интерактивные экскурсии (Экскурсовод)

Экскурсовод — встроенная система обучающих туров, которая проводит пользователя по ключевым интерфейсам платформы. Каждый тур состоит из **шагов** (навигация + подсветка элемента) и **деталей** (объяснение каждого UI-элемента).

> [!TIP] Как запустить
> Нажмите кнопку 🧭 **Compass** в левой боковой панели и выберите экскурсию из каталога.

---

## 1. Знакомство с Hydra (Welcome)

Обзор основных разделов платформы. 5 шагов, 7 элементов.

| Шаг | Описание |
|-----|----------|
| Навигация | Левая боковая панель с разделами |
| Панель экспертов | Центр мультиагентных обсуждений |
| Задачи | Менеджер задач и сессий |
| Штат | Управление ИИ-ролями |
| Библиотеки | Промпты и инструменты |

---

## 2. Менеджер задач

Создание задач, файлы и настройка ИИ-команды. 6 шагов, 23 элемента.

### Список задач
- **Строки задач** — каждая строка: название, описание, количество файлов, время
- **Кнопка «Новая задача»** — создание задачи
- **Активная задача** — выделена цветом

### Выбор задачи
- Клик по задаче открывает детали справа

### Заголовок задачи
- **Название задачи** — редактируется inline
- **Описание** — расширяемое текстовое поле
- **Иконка статуса** — текущий этап задачи

### Настройки модели
- **Мультимодельный селектор** — выбор нескольких моделей для параллельных ответов
- **D-Chat селектор** — модель для приватных консультаций
- **Prompt Engineer** — кнопка оптимизации системных промптов

### Файлы задачи
- **Загрузка файлов** — drag & drop или кнопка
- **Список файлов** — превью, размер, кнопка удаления
- **Потоковая передача** — индикатор загрузки

### Действия
- **Архивировать** — скрыть завершённую задачу
- **Удалить** — безвозвратное удаление задачи

---

## 3. Панель экспертов

Пошаговое руководство по мультиагентному обсуждению. 5 шагов, 20 элементов.

### Выбор моделей
- **Модель Эксперта** — формулирует основной ответ
- **Модель Критика** — анализирует ошибки и слабые места
- **Модель Арбитра** — выносит итоговую оценку
- **Выбор сессии** — переключение между задачами

### Навигатор участников
- **Узлы дерева** — каждый узел = сообщение, цвет = роль
- **Фильтры участников** — клик по имени фильтрует дерево
- **Свернуть навигатор** — компактный режим панели

### Область обсуждения
- **Карточка сообщения** — аватар роли, модель, текст, действия
- **Кнопка 🧠** — вызов Модератора для анализа
- **Кнопка 📦** — сохранение в ролевую память
- **Кнопка ⚖️** — запрос оценки Арбитра
- **Разделитель дат** — группировка по дням

### Ввод запроса
- **Поле ввода** — Markdown, многострочный ввод
- **Вложения** — файлы и изображения
- **Отправка** — запуск обработки ИИ-командой
- **Таймаут** — максимальное время ожидания
- **Пожелания Супервайзера** — специальные инструкции

### D-Chat (Консультант)
- **Селектор модели** — выбор модели и роли для приватного диалога
- **Поле D-Chat** — ввод для приватной консультации
- **История D-Chat** — лента приватных сообщений

---

## 4. Штат специалистов

Настройка ролей, промптов и поведения ИИ-команды. 9 шагов, 29 элементов.

### Список ролей
- **Экспертные роли** — 6 ролей для анализа и генерации
- **Технические роли** — 6 ролей для автоматизации
- **Индикатор доступности** — зелёная галочка при настроенном ключе

### Выбор роли
- Клик по роли открывает карточку справа

### Заголовок роли
- **Аватар и имя** — иконка и название роли
- **Бейдж типа** — экспертная или техническая
- **Описание** — назначение роли

### Системный промпт
- **Секционный просмотр** — промпт разбит на визуальные секции
- **Кнопка редактирования** — переключение в режим секционного редактора
- **Полный просмотр** — промпт в полноэкранном диалоге
- **Библиотека промптов** — загрузка из библиотеки (личные + публичные)
- **Перевод RU↔EN** — автоматический перевод промпта
- **Сохранение** — запись в библиотеку с именем и флагом «Поделиться»

> [!TIP] Индикатор изменений
> Точка рядом с заголовком секции сигнализирует о несохранённых изменениях.

### Табель о рангах (Иерархия)
- **Вкладка «Уступает»** — подчинение другим ролям
- **Вкладка «Оспаривает»** — допустимые дискуссии
- **Вкладка «Сотрудничает»** — совместная работа
- **Автоматическая валидация** — обнаружение и разрешение конфликтов

> [!WARNING] Конфликты
> Система автоматически обнаруживает противоречия и предлагает разрешение перед сохранением.

### Профильные знания (RAG)
- **Ручной ввод** — текст с метаданными (теги, источник)
- **Импорт из Гидрапедии** — загрузка статей
- **Firecrawl-скрейпинг** — импорт веб-страниц
- **Статистика чанков** — количество и статус обучения

### Настройки
- **Утверждение Супервизором** — Human-in-the-Loop
- **Бейдж техника** — маркер технической роли

---

## 5. Подиум ИИ-моделей

Портфолио, конкурсы и рейтинги моделей. 5 шагов, 24 элемента.

### Навигатор моделей
- **Список провайдеров** — OpenAI, Anthropic, Google, DeepSeek и др.
- **Карточки моделей** — имя, провайдер, статус доступности
- **Поиск модели** — фильтрация по названию
- **Индикатор ключа** — наличие API-ключа для провайдера

### Портфолио модели (Досье)
- **Заголовок модели** — имя, провайдер, серия
- **Метрики** — ответы, 🧠 рейтинг, отклонения, средний балл
- **Контекстное окно** — размер окна в токенах
- **Поддержка возможностей** — vision, tools, streaming

### Конкурс красоты ИИ-интеллекта
- **Правила конкурса** — критерии и веса для оценки
- **Задание** — текст для сравнительного тестирования
- **Кандидаты** — список участвующих моделей
- **Подиум** — 🥇🥈🥉 три лучших ответа

### Рейтинги
- **Bar/Pie графики** — визуализация топ-10
- **Фильтр периодов** — неделя, месяц, всё время
- **Таблица рейтинга** — сортируемые колонки метрик
- **Оценки Арбитра** — автоматические оценки качества

### Итоги конкурса
- **Сводка результатов** — подробный разбор ответов
- **Детали кандидата** — полный текст и оценка

---

## 6. Редактор потоков мысли

Визуальное проектирование логики ИИ. 5 шагов, 20 элементов.

### Панель инструментов
- **Имя диаграммы** — редактируется inline
- **Стиль связей** — прямые, ломаные, Безье
- **Undo / Redo** — отмена/повтор действий
- **Авто-раскладка** — алгоритм Dagre
- **Логистика** — ИИ-анализ и оптимизация диаграммы

### Палитра узлов
- **Категории узлов** — Core, AI, Logic, Data
- **Перетаскивание** — drag & drop на холст

### Холст диаграммы
- **Область холста** — масштабирование и перемещение
- **Порты узлов** — входы/выходы для соединений
- **Метки связей** — условия и ярлыки на линиях
- **Панель свойств узла** — настройки выбранного узла
- **Обход узла (Bypass)** — пропуск при выполнении
- **Удаление узла** — удаление с очисткой связей
- **Формы настройки типов** — специализированные формы для каждого типа

### Сохранение и экспорт
- **Сохранить** — запись диаграммы в БД
- **Экспорт** — PNG/JSON/PDF

### Запуск потока
- **Кнопка «Play»** — запуск выполнения
- **Оверлей выполнения** — анимация прогресса по узлам

---

## 7. Библиотека промптов

Управление системными промптами для ролей. 5 шагов, 16 элементов.

### Список промптов
- **Строки промптов** — название, роль, язык, статус
- **Бейдж роли** — цветная иконка назначенной роли
- **Бейдж языка** — RU/EN индикатор
- **Счётчик использований** — количество загрузок промпта

### Выбор промпта
- Клик по промпту открывает детали справа

### Детали промпта
- **Заголовок** — название, роль, теги
- **Содержимое** — полный текст системного промпта
- **Описание** — краткое пояснение назначения

### Редактирование
- **Inline-редактор** — редактирование текста прямо в панели
- **Теги** — добавление/удаление тегов
- **Язык** — выбор языка промпта
- **Общий доступ** — переключатель is_shared

### Действия
- **Сохранить** — обновление промпта
- **Дублировать** — создание копии
- **Удалить** — удаление промпта

---

## 8. Инструменты ИИ

Библиотека Prompt- и HTTP-инструментов. 6 шагов, 18 элементов.

### Список инструментов
- **Строки инструментов** — имя, тип (Prompt/HTTP), категория
- **Бейдж типа** — Prompt (сиреневый) или HTTP (зелёный)
- **Бейдж категории** — тематическая группа
- **Счётчик использований** — количество вызовов

### Выбор инструмента
- Клик по инструменту открывает детали справа

### Детали инструмента
- **Заголовок** — имя, тип, категория
- **Параметры** — JSON-схема параметров
- **Шаблон промпта** — текст шаблона (для Prompt-типа)
- **HTTP-конфигурация** — URL, метод, заголовки (для HTTP-типа)

### Тестирование
- **Prompt Tool Tester** — запуск с тестовыми параметрами
- **HTTP Tool Tester** — отправка HTTP-запроса с параметрами

### Редактирование
- **Редактор инструмента** — полнофункциональный редактор
- **Общий доступ** — переключатель is_shared

### Действия
- **Сохранить** — обновление инструмента
- **Удалить** — удаление инструмента

---

## 9. Паттерны поведения

Стратегические рецепты и ролевые паттерны. 6 шагов, 16 элементов.

### Список паттернов
- **Строки паттернов** — тип (Blueprint/Behavior), название, категория
- **Группировка по типу** — рецепты и поведения разделены

### Стратегические рецепты
- **Карточки рецептов** — многоэтапные сценарии с ролями

### Ролевые паттерны
- **Карточки поведений** — тон общения, детализация, реакции

### Выбор паттерна
- Клик по паттерну открывает панель деталей

### Детали паттерна
- **Inline-редактор** — редактирование без диалогов
- **Бейджи реакций** — визуальные индикаторы с тултипами
- **Интеграция с потоками** — генерация диаграммы из рецепта
- **Этапы стратегии** — вертикальный таймлайн с ролями
- **Контрольные точки** — проверки после этапов
- **Открыть в редакторе потоков** — автоматическая генерация диаграммы
- **Стиль общения** — тон и детализация ответов
- **Предпочтения формата** — markdown, таблицы, код
- **Табы взаимодействий** — «Уступает», «Сотрудничает», «Оспаривает»

### Создание
- **Создать рецепт** — мастер этапов
- **Создать паттерн** — диалог создания поведения

---

## Механика экскурсий

### Подсветка
Каждый шаг подсвечивает целевой элемент зелёной рамкой с помощью DOM-селектора. Детали панели выделяются голубой рамкой при выборе.

### Навигация
- **Выпадающий список шагов** — быстрый переход к любому шагу
- **Кнопки «Назад» / «Далее»** — последовательное прохождение
- **Авто-скролл** — целевой элемент прокручивается в видимую область

### Детализация
Кнопка **«Объяснить подробнее»** открывает комбо-бокс элементов панели с описанием каждого UI-элемента текущего шага.

> [!EXAMPLE] Пример
> На шаге «Область обсуждения» в Панели экспертов можно раскрыть детали и увидеть описание карточки сообщения, кнопок 🧠/📦/⚖️ и разделителя дат.`,

      en: `# Interactive Tours (Guide)

The Guide — a built-in system of educational tours that walks users through the platform's key interfaces. Each tour consists of **steps** (navigation + element highlighting) and **details** (explanation of each UI element).

> [!TIP] How to Start
> Click the 🧭 **Compass** button in the left sidebar and select a tour from the catalog.

---

## 1. Welcome to Hydra

Overview of the platform's main sections. 5 steps, 7 elements.

| Step | Description |
|------|-------------|
| Navigation | Left sidebar with sections |
| Expert Panel | Multi-agent discussion center |
| Tasks | Task and session manager |
| Staff | AI role management |
| Libraries | Prompts and tools |

---

## 2. Task Manager

Creating tasks, files, and AI team setup. 6 steps, 23 elements.

### Task List
- **Task Rows** — each row: title, description, file count, time
- **"New Task" Button** — create a task
- **Active Task** — highlighted with color

### Task Selection
- Clicking a task opens details on the right

### Task Header
- **Task Title** — editable inline
- **Description** — expandable text field
- **Status Icon** — current task stage

### Model Settings
- **Multi-Model Selector** — choose multiple models for parallel responses
- **D-Chat Selector** — model for private consultations
- **Prompt Engineer** — system prompt optimization button

### Task Files
- **File Upload** — drag & drop or button
- **File List** — preview, size, delete button
- **Streaming** — upload progress indicator

### Actions
- **Archive** — hide completed task
- **Delete** — permanently remove task

---

## 3. Expert Panel

Step-by-step guide to multi-agent discussion. 5 steps, 20 elements.

### Model Selection
- **Expert Model** — formulates the main answer
- **Critic Model** — analyzes errors and weaknesses
- **Arbiter Model** — delivers the final evaluation
- **Session Select** — switch between tasks

### Participant Navigator
- **Tree Nodes** — each node = message, color = role
- **Participant Filters** — click name to filter tree
- **Collapse Navigator** — compact panel mode

### Discussion Area
- **Message Card** — role avatar, model, text, actions
- **🧠 Button** — call Moderator for analysis
- **📦 Button** — save to role memory
- **⚖️ Button** — request Arbiter evaluation
- **Date Separator** — group by day

### Input Query
- **Text Input** — Markdown, multiline
- **Attachments** — files and images
- **Send** — start AI team processing
- **Timeout** — maximum wait time
- **Supervisor Wishes** — special instructions

### D-Chat (Consultant)
- **Model Selector** — choose model and role for private dialogue
- **D-Chat Input** — private consultation input
- **D-Chat History** — private message feed

---

## 4. AI Staff

Role, prompt, and AI team behavior configuration. 9 steps, 29 elements.

### Role List
- **Expert Roles** — 6 roles for analysis and generation
- **Technical Roles** — 6 roles for automation
- **Availability Indicator** — green checkmark with configured key

### Role Selection
- Clicking a role opens the card on the right

### Role Header
- **Avatar and Name** — icon and role name
- **Type Badge** — expert or technical
- **Description** — role purpose

### System Prompt
- **Section View** — prompt split into visual sections
- **Edit Button** — switch to section editor mode
- **Full View** — prompt in fullscreen dialog
- **Prompt Library** — load from library (personal + shared)
- **RU↔EN Translation** — automatic prompt translation
- **Save** — write to library with name and "Share" flag

> [!TIP] Change Indicator
> A dot next to the section heading signals unsaved changes.

### Rank Table (Hierarchy)
- **"Defers to" Tab** — subordination to other roles
- **"Challenges" Tab** — permitted disputes
- **"Collaborates" Tab** — joint work
- **Automatic Validation** — conflict detection and resolution

> [!WARNING] Conflicts
> The system automatically detects contradictions and offers resolution before saving.

### Domain Knowledge (RAG)
- **Manual Input** — text with metadata (tags, source)
- **Import from Hydrapedia** — load articles
- **Firecrawl Scraping** — import web pages
- **Chunk Statistics** — count and training status

### Settings
- **Supervisor Approval** — Human-in-the-Loop
- **Technical Badge** — technical role marker

---

## 5. AI Model Podium

Portfolio, contests, and model ratings. 5 steps, 24 elements.

### Model Navigator
- **Provider List** — OpenAI, Anthropic, Google, DeepSeek, etc.
- **Model Cards** — name, provider, availability status
- **Model Search** — filter by name
- **Key Indicator** — API key presence for provider

### Model Portfolio (Dossier)
- **Model Header** — name, provider, series
- **Metrics** — responses, 🧠 rating, dismissals, average score
- **Context Window** — window size in tokens
- **Capability Support** — vision, tools, streaming

### AI Intelligence Beauty Contest
- **Contest Rules** — criteria and weights for evaluation
- **Task** — text for comparative testing
- **Candidates** — participating models list
- **Podium** — 🥇🥈🥉 top three responses

### Ratings
- **Bar/Pie Charts** — top-10 visualization
- **Period Filter** — week, month, all time
- **Rating Table** — sortable metric columns
- **Arbiter Scores** — automatic quality evaluations

### Contest Summary
- **Results Summary** — detailed response breakdown
- **Candidate Details** — full text and score

---

## 6. Thought Flow Editor

Visual AI logic design. 5 steps, 20 elements.

### Toolbar
- **Diagram Name** — editable inline
- **Edge Style** — straight, step, Bezier
- **Undo / Redo** — undo/redo actions
- **Auto Layout** — Dagre algorithm
- **Logistics** — AI analysis and diagram optimization

### Node Palette
- **Node Categories** — Core, AI, Logic, Data
- **Drag Handle** — drag & drop onto canvas

### Diagram Canvas
- **Canvas Area** — zoom and pan
- **Node Ports** — input/output connection points
- **Edge Labels** — conditions and labels on lines
- **Node Properties Panel** — selected node settings
- **Node Bypass** — skip during execution
- **Delete Node** — remove with edge cleanup
- **Type-Specific Forms** — specialized forms for each node type

### Save & Export
- **Save** — write diagram to database
- **Export** — PNG/JSON/PDF

### Flow Execution
- **Play Button** — start execution
- **Execution Overlay** — progress animation across nodes

---

## 7. Prompt Library

System prompt management for roles. 5 steps, 16 elements.

### Prompt List
- **Prompt Rows** — name, role, language, status
- **Role Badge** — colored icon for assigned role
- **Language Badge** — RU/EN indicator
- **Usage Counter** — prompt load count

### Prompt Selection
- Clicking a prompt opens details on the right

### Prompt Details
- **Header** — name, role, tags
- **Content** — full system prompt text
- **Description** — brief purpose explanation

### Editing
- **Inline Editor** — edit text directly in panel
- **Tags** — add/remove tags
- **Language** — prompt language selection
- **Shared Access** — is_shared toggle

### Actions
- **Save** — update prompt
- **Duplicate** — create a copy
- **Delete** — remove prompt

---

## 8. AI Tools

Prompt and HTTP tool library. 6 steps, 18 elements.

### Tool List
- **Tool Rows** — name, type (Prompt/HTTP), category
- **Type Badge** — Prompt (purple) or HTTP (green)
- **Category Badge** — thematic group
- **Usage Counter** — call count

### Tool Selection
- Clicking a tool opens details on the right

### Tool Details
- **Header** — name, type, category
- **Parameters** — JSON parameter schema
- **Prompt Template** — template text (for Prompt type)
- **HTTP Configuration** — URL, method, headers (for HTTP type)

### Testing
- **Prompt Tool Tester** — run with test parameters
- **HTTP Tool Tester** — send HTTP request with parameters

### Editing
- **Tool Editor** — full-featured editor
- **Shared Access** — is_shared toggle

### Actions
- **Save** — update tool
- **Delete** — remove tool

---

## 9. Behavioral Patterns

Strategic blueprints and role patterns. 6 steps, 16 elements.

### Pattern List
- **Pattern Rows** — type (Blueprint/Behavior), name, category
- **Type Groups** — blueprints and behaviors separated

### Strategic Blueprints
- **Blueprint Cards** — multi-stage scenarios with roles

### Role Behaviors
- **Behavior Cards** — communication tone, verbosity, reactions

### Pattern Selection
- Clicking a pattern opens the details panel

### Pattern Details
- **Inline Editor** — edit without dialogs
- **Reaction Badges** — visual indicators with tooltips
- **Flow Integration** — generate diagram from blueprint
- **Strategy Stages** — vertical timeline with roles
- **Checkpoints** — verification after stages
- **Open in Flow Editor** — automatic diagram generation
- **Communication Style** — tone and response verbosity
- **Format Preferences** — markdown, tables, code
- **Interaction Tabs** — "Defers to", "Collaborates", "Challenges"

### Creation
- **Create Blueprint** — stage wizard
- **Create Behavior** — behavior creation dialog

---

## Tour Mechanics

### Highlighting
Each step highlights the target element with a green frame using a DOM selector. Panel details are highlighted with a blue frame when selected.

### Navigation
- **Step Dropdown** — quick jump to any step
- **Back / Next Buttons** — sequential progression
- **Auto-scroll** — target element scrolls into view

### Details
The **"Explain More"** button opens a combo-box of panel elements with a description of each UI element for the current step.

> [!EXAMPLE] Example
> On the "Discussion Area" step in the Expert Panel, you can expand details to see descriptions of the message card, 🧠/📦/⚖️ buttons, and date separator.`,
    },
  },
];
